export default function(state = null, action) {
  switch(action.type) {
    case 'UPVOTE':
    case 'DOWNVOTE':
      // unshift votables and make votables[0] new currentVotable
  }

  return state
}
